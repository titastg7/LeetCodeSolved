326. Power of Three


C++ Code
----------------------------------

class Solution {
public:
    bool isPowerOfThree(int n) {
        if(n<=0)
            return 0;
        long long largest = pow(3,19);
        return (largest%n==0?1:0);
    }
};
